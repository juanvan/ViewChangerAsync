<Window x:Class="ViewChangerAsync.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ViewChangerAsync.Views"
        xmlns:cal="http://caliburnmicro.com"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">
   
        <DockPanel>
            <Menu DockPanel.Dock="Top" FontSize="18">
                <MenuItem Header="Views">
                <MenuItem x:Name="ChangeView" Header=" ChangeView"/>
                </MenuItem>
            
            </Menu>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ContentControl x:Name="NavigationUserControl" cal:View.Model="{Binding NavigationUserControl, Mode=TwoWay}" cal:View.Context="NavigationView" Grid.Row="1" />
        <ContentControl x:Name="ActiveItem" Grid.Row="2" />
    </Grid>
        </DockPanel>
</Window>
